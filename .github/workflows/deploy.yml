- name: Stop and remove old container
  run: |
    docker rm -f dice-app || true

- name: Build Docker image
  run: docker build -t dice-app .

- name: Run container
  run: |
    docker run -d -p 80:80 --name dice-app dice-app || docker logs dice-app
